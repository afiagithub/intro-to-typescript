var tUser;
tUser = ['afia', 123, true];
var rgb = [255, 255, 255];
var newUser = ['liz', 445];
newUser[1] = 888;
// we can push, pop, shift, unshift element in newUser, which shouldn't be allowed
newUser.pop();
